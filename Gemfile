#!/usr/bin/env ruby

source :gemcutter

gem 'net-ssh', :require => 'net/ssh'
gem 'net-ping'

group :test do
  gem 'minitest'
  gem 'rspec', '1.3.0', :require => 'spec'
  gem 'net-ssh', :require => 'net/ssh'  
end
